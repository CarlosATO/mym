'use client'

import { useEffect, useState } from 'react'
import { AlertCircle, ArrowLeft, Plus, RefreshCw, Save, Settings2, SlidersHorizontal, UsersRound, X } from 'lucide-react'
import {
  deactivateCommissionRule, getCommissionGroups, getCommissionRules, getCommissionSettings, getCommissionSellers,
  previewCommissionSettlement, searchCommissionProducts, searchCommissionSuppliers, updateCommissionSettings,
  upsertCommissionGroup, upsertCommissionRule, upsertCommissionSellerProfile,
  type CommissionGroup, type CommissionPreview, type CommissionPreviewLine, type CommissionRule, type CommissionRuleScope,
  type CommissionRuleType, type CommissionSeller, type CommissionSellerProfileInput, type CommissionSellerType, type CommissionSettings,
} from '@/app/actions/comercial/commissions'
import { cn } from '@/lib/utils'

type View = 'main' | 'configuration'
type ConfigTab = 'sellers' | 'general' | 'groups' | 'rules'
type SellerDraft = Omit<CommissionSellerProfileInput, 'seller_bsale_id'>

const sellerTypes: Array<{ value: CommissionSellerType; label: string }> = [
  { value: 'FIELD', label: 'Terreno' }, { value: 'ADMIN', label: 'Administración' }, { value: 'MANAGEMENT', label: 'Gerencia' }, { value: 'DISPATCH', label: 'Despacho' }, { value: 'OTHER', label: 'Otro' },
]
const emptyRule = { rule_scope: 'GENERAL' as CommissionRuleScope, rule_type: 'FIXED_PERCENT' as CommissionRuleType, seller_profile_id: '', supplier_id: '', commission_group_id: '', product_id: '', commission_percent: '1', min_amount: '', max_amount: '', min_quantity: '', max_quantity: '', valid_from: '2026-06-26', valid_to: '', priority: '0', notes: '' }

function today() { return new Date().toISOString().slice(0, 10) }
function money(value: number) { return `$${value.toLocaleString('es-CL', { maximumFractionDigits: 0 })}` }
function errorMessage(error: unknown) { return error instanceof Error ? error.message : 'No se pudo completar la operación' }
function sellerDraft(seller: CommissionSeller): SellerDraft { return { seller_name: seller.seller_name || '', is_commissionable: seller.is_commissionable, seller_type: seller.seller_type, active: seller.profile_active ?? true, notes: seller.notes || '' } }

export function CommissionsPanel() {
  const [view, setView] = useState<View>('main')
  const [tab, setTab] = useState<ConfigTab>('sellers')
  const [sellers, setSellers] = useState<CommissionSeller[]>([])
  const [drafts, setDrafts] = useState<Record<number, SellerDraft>>({})
  const [settings, setSettings] = useState<CommissionSettings | null>(null)
  const [groups, setGroups] = useState<CommissionGroup[]>([])
  const [rules, setRules] = useState<CommissionRule[]>([])
  const [suppliers, setSuppliers] = useState<Array<{ id: string; name: string }>>([])
  const [products, setProducts] = useState<Array<{ id: string; sku: string; description: string }>>([])
  const [sellerId, setSellerId] = useState('')
  const [periodFrom, setPeriodFrom] = useState('2026-06-26')
  const [periodTo, setPeriodTo] = useState(today)
  const [preview, setPreview] = useState<CommissionPreview | null>(null)
  const [groupForm, setGroupForm] = useState({ code: '', name: '', description: '' })
  const [ruleForm, setRuleForm] = useState(emptyRule)
  const [loading, setLoading] = useState(true)
  const [busy, setBusy] = useState(false)
  const [error, setError] = useState<string | null>(null)

  const loadSellers = async () => {
    setLoading(true); setError(null)
    try { const rows = await getCommissionSellers(); setSellers(rows); setDrafts(Object.fromEntries(rows.map(row => [row.seller_bsale_id, sellerDraft(row)]))) }
    catch (err) { setError(errorMessage(err)) } finally { setLoading(false) }
  }
  const loadConfig = async () => {
    setBusy(true); setError(null)
    try {
      const [nextSettings, nextGroups, nextRules, nextSuppliers] = await Promise.all([getCommissionSettings(), getCommissionGroups(), getCommissionRules(), searchCommissionSuppliers('')])
      setSettings(nextSettings); setGroups(nextGroups); setRules(nextRules); setSuppliers(nextSuppliers)
    } catch (err) { setError(errorMessage(err)) } finally { setBusy(false) }
  }
  useEffect(() => { const handle = setTimeout(() => { void loadSellers() }, 0); return () => clearTimeout(handle) }, [])

  const commissionable = sellers.filter(seller => seller.is_commissionable && seller.profile_active === true)
  const openConfig = () => { setView('configuration'); void loadConfig() }
  const simulate = async (id = sellerId) => {
    if (!id) return
    if (id !== sellerId) return
    setBusy(true); setError(null)
    try { const result = await previewCommissionSettlement({ seller_bsale_id: Number(id), period_from: periodFrom, period_to: periodTo }); setPreview(result); if (result.summary.period_from) setPeriodFrom(result.summary.period_from) }
    catch (err) { setError(errorMessage(err)); setPreview(null) } finally { setBusy(false) }
  }
  const saveSeller = async (seller: CommissionSeller) => {
    const input = drafts[seller.seller_bsale_id]; if (!input) return
    setBusy(true); try { await upsertCommissionSellerProfile({ seller_bsale_id: seller.seller_bsale_id, ...input }); await loadSellers() } catch (err) { setError(errorMessage(err)) } finally { setBusy(false) }
  }

  return <div className="commission-panel flex h-full flex-col overflow-hidden bg-theme-surface text-theme-text">
    <Header view={view} onConfig={openConfig} onBack={() => setView('main')} />
    {error && <div className="mx-4 mt-3 flex items-center gap-2 rounded-lg border border-red-500/30 bg-red-500/10 px-3 py-2 text-sm text-theme-text"><AlertCircle className="h-4 w-4 text-red-600" />{error}</div>}
    {view === 'main' ? <main className="flex-1 overflow-auto px-4 py-4 md:px-6">{loading ? <Loading /> : commissionable.length === 0 ? <Empty onConfig={openConfig} /> : <div className="w-full space-y-4"><section className="rounded-xl border border-theme-border bg-theme-bg/35 p-4"><div className="grid gap-3 md:grid-cols-[1fr_150px_150px_auto]"><Field label="Vendedor"><select value={sellerId} onChange={event => { setSellerId(event.target.value); setPreview(null); if (event.target.value) void simulate(event.target.value) }}><option value="">Selecciona un vendedor</option>{commissionable.map(seller => <option key={seller.seller_bsale_id} value={seller.seller_bsale_id}>{seller.seller_name}</option>)}</select></Field><Field label="Desde"><input type="date" value={periodFrom} max={periodTo} onChange={event => { setPeriodFrom(event.target.value); setPreview(null) }} /></Field><Field label="Hasta"><input type="date" value={periodTo} min={periodFrom} max={today()} onChange={event => { setPeriodTo(event.target.value); setPreview(null) }} /></Field><button disabled={!sellerId || busy} onClick={() => void simulate()} className="btn-primary self-end">{busy ? 'Simulando...' : 'Simular'}</button></div></section>{preview && <PreviewReport preview={preview} />}</div>}</main> : <Configuration tab={tab} setTab={setTab} sellers={sellers} drafts={drafts} settings={settings} groups={groups} rules={rules} suppliers={suppliers} products={products} groupForm={groupForm} ruleForm={ruleForm} busy={busy} onSellerChange={(id, changes) => setDrafts(current => ({ ...current, [id]: { ...current[id], ...changes } }))} onSaveSeller={saveSeller} onSettingsChange={percent => setSettings(current => current ? { ...current, default_commission_percent: percent } : current)} onSaveSettings={async () => { if (!settings) return; setBusy(true); try { setSettings(await updateCommissionSettings({ default_commission_percent: settings.default_commission_percent })) } catch (err) { setError(errorMessage(err)) } finally { setBusy(false) } }} onGroupChange={changes => setGroupForm(current => ({ ...current, ...changes }))} onSaveGroup={async () => { setBusy(true); try { await upsertCommissionGroup({ ...groupForm, supplier_id: null, parent_supplier_id: null, is_active: true }); setGroupForm({ code: '', name: '', description: '' }); setGroups(await getCommissionGroups()) } catch (err) { setError(errorMessage(err)) } finally { setBusy(false) } }} onRuleChange={changes => setRuleForm(current => ({ ...current, ...changes }))} onSearchProducts={async query => setProducts(await searchCommissionProducts(query))} onSaveRule={async () => { setBusy(true); try { await upsertCommissionRule({ rule_scope: ruleForm.rule_scope, rule_type: ruleForm.rule_type, seller_profile_id: ruleForm.seller_profile_id || null, supplier_id: ruleForm.supplier_id || null, commission_group_id: ruleForm.commission_group_id || null, product_id: ruleForm.product_id || null, commission_percent: Number(ruleForm.commission_percent), min_amount: ruleForm.min_amount ? Number(ruleForm.min_amount) : null, max_amount: ruleForm.max_amount ? Number(ruleForm.max_amount) : null, min_quantity: ruleForm.min_quantity ? Number(ruleForm.min_quantity) : null, max_quantity: ruleForm.max_quantity ? Number(ruleForm.max_quantity) : null, valid_from: ruleForm.valid_from, valid_to: ruleForm.valid_to || null, priority: Number(ruleForm.priority), is_active: true, notes: ruleForm.notes }); setRuleForm(emptyRule); setRules(await getCommissionRules()) } catch (err) { setError(errorMessage(err)) } finally { setBusy(false) } }} onDeactivateRule={async id => { setBusy(true); try { await deactivateCommissionRule(id); setRules(await getCommissionRules()) } catch (err) { setError(errorMessage(err)) } finally { setBusy(false) } }} />}
  </div>
}

function Header({ view, onConfig, onBack }: { view: View; onConfig: () => void; onBack: () => void }) { return <><header className="shrink-0 border-b border-theme-border px-4 py-3 md:px-5"><div className="flex items-center justify-between gap-3"><div><div className="flex items-center gap-2"><UsersRound className="h-4 w-4 text-theme-accent" /><h2 className="font-accent text-lg font-semibold">Comisiones de Vendedores</h2></div><p className="mt-1 text-sm text-theme-text-muted">{view === 'main' ? 'Simulación. No emite liquidación ni bloquea facturas.' : 'Configuración de vendedores, comisión general, grupos y reglas.'}</p></div>{view === 'main' ? <button onClick={onConfig} className="btn-secondary"><Settings2 className="h-3.5 w-3.5" />Configuración</button> : <button onClick={onBack} className="btn-secondary"><ArrowLeft className="h-3.5 w-3.5" />Volver a comisiones</button>}</div></header><CommissionStyles /></> }
function CommissionStyles() { return <style jsx global>{`.commission-panel .btn-primary,.commission-panel .btn-secondary{display:inline-flex;align-items:center;justify-content:center;gap:.375rem;min-height:2rem;border-radius:.5rem;padding:.35rem .7rem;font-size:.75rem;font-weight:700}.commission-panel .btn-primary{background:var(--theme-accent);color:#fff}.commission-panel .btn-secondary{border:1px solid var(--theme-border);background:var(--theme-surface);color:var(--theme-text)}.commission-panel input,.commission-panel select{width:100%;border:1px solid var(--theme-border);border-radius:.5rem;background:var(--theme-surface);color:var(--theme-text);padding:.45rem .6rem;font-size:.75rem}.commission-panel table,.commission-panel th,.commission-panel td{color:var(--theme-text)!important}.commission-panel th{background:var(--theme-bg);padding:.55rem .6rem;text-align:left}.commission-panel td{padding:.55rem .6rem;border-top:1px solid var(--theme-border)}.commission-panel tbody tr:hover{background:var(--theme-surface-hover)}`}</style> }
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="block text-[11px] font-semibold uppercase tracking-wide text-theme-text-muted">{label}<span className="mt-1 block">{children}</span></label> }
function Loading() { return <div className="flex h-40 items-center justify-center"><RefreshCw className="h-5 w-5 animate-spin text-theme-accent" /></div> }
function Empty({ onConfig }: { onConfig: () => void }) { return <section className="mx-auto mt-10 max-w-xl rounded-xl border border-theme-border bg-theme-bg/35 p-7 text-center"><UsersRound className="mx-auto h-8 w-8 text-theme-text-muted" /><h3 className="mt-4 font-semibold">No hay vendedores comisionables configurados.</h3><p className="mt-2 text-sm text-theme-text-muted">Configura vendedores para comenzar a simular comisiones.</p><button onClick={onConfig} className="btn-primary mt-5"><Settings2 className="h-3.5 w-3.5" />Configurar vendedores</button></section> }

function PreviewReport({ preview }: { preview: CommissionPreview }) {
  const [filters, setFilters] = useState({ invoice: '', supplier: '', product: '', percent: '' })
  const filtered = preview.lines.filter(line => {
    const invoice = String(line.invoice_number || line.invoice_bsale_id).includes(filters.invoice.trim())
    const supplier = `${line.supplier_name || ''} ${line.commission_group_name || ''}`.toLowerCase().includes(filters.supplier.trim().toLowerCase())
    const product = `${line.sku || ''} ${line.product_name || ''}`.toLowerCase().includes(filters.product.trim().toLowerCase())
    const percent = !filters.percent || String(line.commission_percent) === filters.percent
    return invoice && supplier && product && percent
  })
  const net = filtered.reduce((sum, line) => sum + line.net_amount, 0)
  const commission = filtered.reduce((sum, line) => sum + line.commission_amount, 0)
  const invoices = new Set(filtered.map(line => line.invoice_bsale_id)).size
  const general = filtered.filter(line => line.warning_code === 'DEFAULT_RULE_USED').length
  const average = net ? commission / net * 100 : 0
  const percentages = Array.from(new Set(preview.lines.map(line => line.commission_percent))).sort((a, b) => a - b)
  const clear = () => setFilters({ invoice: '', supplier: '', product: '', percent: '' })
  return <section className="space-y-3"><div className="rounded-xl border border-theme-border bg-theme-bg/35 px-3 py-2"><div className="grid gap-x-5 gap-y-1 sm:grid-cols-2 lg:grid-cols-5"><Kpi label="Facturas" value={invoices.toLocaleString('es-CL')} /><Kpi label="Líneas" value={filtered.length.toLocaleString('es-CL')} /><Kpi label="Neto" value={money(net)} /><Kpi label="Comisión estimada" value={money(commission)} /><Kpi label="% efectivo" value={`${average.toFixed(2)}%`} /></div></div><div className="rounded-xl border border-theme-border bg-theme-surface p-3"><div className="mb-2 flex items-center gap-2 text-xs font-semibold text-theme-text"><SlidersHorizontal className="h-3.5 w-3.5 text-theme-accent" />Filtros de simulación</div><div className="grid gap-2 md:grid-cols-2 xl:grid-cols-[180px_1fr_1fr_140px_auto]"><input value={filters.invoice} onChange={e => setFilters(current => ({ ...current, invoice: e.target.value }))} placeholder="Factura" /><input value={filters.supplier} onChange={e => setFilters(current => ({ ...current, supplier: e.target.value }))} placeholder="Proveedor o grupo" /><input value={filters.product} onChange={e => setFilters(current => ({ ...current, product: e.target.value }))} placeholder="SKU o producto" /><select value={filters.percent} onChange={e => setFilters(current => ({ ...current, percent: e.target.value }))}><option value="">Todos los %</option>{percentages.map(percent => <option key={percent} value={percent}>{percent}%</option>)}</select><button onClick={clear} className="btn-secondary"><X className="h-3.5 w-3.5" />Limpiar</button></div></div>{general > 0 && <div className="rounded-lg border border-theme-border bg-theme-bg px-3 py-2 text-xs text-theme-text"><b>{general}</b> usando comisión general para línea sin regla específica.</div>}<PreviewTable lines={filtered} /></section>
}
function Kpi({ label, value }: { label: string; value: string }) { return <div className="flex items-baseline justify-between gap-2 border-b border-theme-border py-1.5 last:border-0 lg:block lg:border-0"><span className="text-[10px] font-semibold uppercase tracking-wide text-theme-text-muted">{label}</span><span className="text-base font-bold">{value}</span></div> }
function PreviewTable({ lines }: { lines: CommissionPreviewLine[] }) { return <div className="overflow-auto rounded-xl border border-theme-border"><table className="min-w-[1180px] w-full text-xs"><thead><tr><th>Factura</th><th>Cliente</th><th>Pago</th><th>SKU / Producto</th><th>Proveedor / Grupo</th><th className="text-right">Cant.</th><th className="text-right">Neto</th><th>Regla</th><th className="text-right">%</th><th className="text-right">Comisión</th></tr></thead><tbody>{lines.map(line => <tr key={line.invoice_line_id}><td>{line.invoice_number || line.invoice_bsale_id}</td><td className="max-w-[180px] truncate">{line.customer_name}</td><td>{line.payment_completed_at?.slice(0, 10)}</td><td><b>{line.sku}</b><div>{line.product_name}</div></td><td><div>{line.supplier_name}</div><div>{line.commission_group_name || 'Sin grupo'}</div></td><td className="text-right">{line.quantity}</td><td className="text-right">{money(line.net_amount)}</td><td>{line.rule_scope}</td><td className="text-right">{line.commission_percent}%</td><td className="text-right font-semibold">{money(line.commission_amount)}</td></tr>)}</tbody></table>{lines.length === 0 && <div className="p-6 text-center text-sm text-theme-text-muted">No hay líneas que coincidan con los filtros.</div>}</div> }

function Configuration(props: { tab: ConfigTab; setTab: (tab: ConfigTab) => void; sellers: CommissionSeller[]; drafts: Record<number, SellerDraft>; settings: CommissionSettings | null; groups: CommissionGroup[]; rules: CommissionRule[]; suppliers: Array<{ id: string; name: string }>; products: Array<{ id: string; sku: string; description: string }>; groupForm: { code: string; name: string; description: string }; ruleForm: typeof emptyRule; busy: boolean; onSellerChange: (id: number, changes: Partial<SellerDraft>) => void; onSaveSeller: (seller: CommissionSeller) => void; onSettingsChange: (percent: number) => void; onSaveSettings: () => void; onGroupChange: (changes: Partial<{ code: string; name: string; description: string }>) => void; onSaveGroup: () => void; onRuleChange: (changes: Partial<typeof emptyRule>) => void; onSearchProducts: (query: string) => Promise<void>; onSaveRule: () => void; onDeactivateRule: (id: string) => void }) { const { tab, setTab } = props; return <main className="min-h-0 flex-1 overflow-auto"><div className="flex gap-1 border-b border-theme-border px-4 pt-3">{(['sellers','general','groups','rules'] as ConfigTab[]).map(item => <button key={item} onClick={() => setTab(item)} className={cn('rounded-t-lg px-3 py-2 text-xs font-semibold', tab === item ? 'bg-theme-accent-muted text-theme-text' : 'text-theme-text-muted hover:bg-theme-surface-hover')}>{({ sellers: 'Vendedores', general: 'General', groups: 'Grupos', rules: 'Reglas' })[item]}</button>)}</div><div className="p-4">{tab === 'sellers' ? <SellerTable {...props} /> : tab === 'general' ? <General {...props} /> : tab === 'groups' ? <Groups {...props} /> : <Rules {...props} />}</div></main> }
function SellerTable({ sellers, drafts, busy, onSellerChange, onSaveSeller }: Pick<Parameters<typeof Configuration>[0], 'sellers' | 'drafts' | 'busy' | 'onSellerChange' | 'onSaveSeller'>) { return <div className="overflow-auto rounded-xl border border-theme-border"><table className="min-w-[900px] w-full text-xs"><thead><tr><th>Vendedor</th><th>Tipo</th><th>Comisionable</th><th>Activo</th><th>Notas</th><th></th></tr></thead><tbody>{sellers.map(seller => { const row = drafts[seller.seller_bsale_id] || sellerDraft(seller); return <tr key={seller.seller_bsale_id}><td><b>{seller.seller_name}</b><div>{seller.paid_invoices_count} facturas pagadas</div></td><td><select value={row.seller_type} onChange={e => onSellerChange(seller.seller_bsale_id, { seller_type: e.target.value as CommissionSellerType })}>{sellerTypes.map(type => <option key={type.value} value={type.value}>{type.label}</option>)}</select></td><td className="text-center"><input type="checkbox" checked={row.is_commissionable} onChange={e => onSellerChange(seller.seller_bsale_id, { is_commissionable: e.target.checked })} /></td><td className="text-center"><input type="checkbox" checked={row.active} onChange={e => onSellerChange(seller.seller_bsale_id, { active: e.target.checked })} /></td><td><input value={row.notes} onChange={e => onSellerChange(seller.seller_bsale_id, { notes: e.target.value })} /></td><td><button disabled={busy} onClick={() => onSaveSeller(seller)} className="btn-primary"><Save className="h-3.5 w-3.5" />Guardar</button></td></tr> })}</tbody></table></div> }
function General({ settings, busy, onSettingsChange, onSaveSettings }: Pick<Parameters<typeof Configuration>[0], 'settings' | 'busy' | 'onSettingsChange' | 'onSaveSettings'>) { if (!settings) return <Loading />; return <div className="w-full rounded-xl border border-theme-border bg-theme-bg/30 p-4"><div className="grid gap-3 md:grid-cols-4"><Field label="Comisión general (%)"><input type="number" min="0" max="100" step="0.01" value={settings.default_commission_percent} onChange={e => onSettingsChange(Number(e.target.value))} /></Field><div className="text-sm"><b>Base:</b> NET<br /><b>Pago completo:</b> Sí</div><div className="text-sm"><b>Cierre histórico:</b> {settings.historical_cutoff_date}<br /><b>Primer día elegible:</b> {settings.first_eligible_date}</div><button disabled={busy} onClick={onSaveSettings} className="btn-primary self-end"><Save className="h-3.5 w-3.5" />Guardar</button></div></div> }
function Groups({ groups, groupForm, busy, onGroupChange, onSaveGroup }: Pick<Parameters<typeof Configuration>[0], 'groups' | 'groupForm' | 'busy' | 'onGroupChange' | 'onSaveGroup'>) { return <div className="w-full space-y-3"><div className="grid gap-2 rounded-xl border border-theme-border bg-theme-bg/30 p-4 md:grid-cols-3"><Field label="Código"><input value={groupForm.code} onChange={e => onGroupChange({ code: e.target.value })} /></Field><Field label="Nombre"><input value={groupForm.name} onChange={e => onGroupChange({ name: e.target.value })} /></Field><Field label="Descripción"><input value={groupForm.description} onChange={e => onGroupChange({ description: e.target.value })} /></Field><button disabled={busy} onClick={onSaveGroup} className="btn-primary md:col-span-3"><Plus className="h-3.5 w-3.5" />Crear grupo</button></div>{groups.map(group => <div key={group.id} className="rounded-lg border border-theme-border p-3"><b>{group.name}</b><span className="ml-2 text-theme-text-muted">{group.code}</span></div>)}</div> }
function Rules({ rules, groups, sellers, suppliers, products, ruleForm, busy, onRuleChange, onSearchProducts, onSaveRule, onDeactivateRule }: Pick<Parameters<typeof Configuration>[0], 'rules' | 'groups' | 'sellers' | 'suppliers' | 'products' | 'ruleForm' | 'busy' | 'onRuleChange' | 'onSearchProducts' | 'onSaveRule' | 'onDeactivateRule'>) { const scope = ruleForm.rule_scope; return <div className="w-full space-y-3"><div className="grid gap-2 rounded-xl border border-theme-border bg-theme-bg/30 p-4 md:grid-cols-3"><Field label="Ámbito"><select value={scope} onChange={e => onRuleChange({ rule_scope: e.target.value as CommissionRuleScope, supplier_id: '', commission_group_id: '', product_id: '' })}><option value="GENERAL">General</option><option value="SUPPLIER">Proveedor</option><option value="GROUP">Grupo</option><option value="PRODUCT">Producto</option></select></Field><Field label="Tipo"><select value={ruleForm.rule_type} onChange={e => onRuleChange({ rule_type: e.target.value as CommissionRuleType })}><option value="FIXED_PERCENT">Porcentaje fijo</option><option value="RANGE_BY_AMOUNT">Rango por monto</option><option value="RANGE_BY_QUANTITY">Rango por cantidad</option></select></Field><Field label="Porcentaje"><input type="number" value={ruleForm.commission_percent} onChange={e => onRuleChange({ commission_percent: e.target.value })} /></Field>{scope === 'SUPPLIER' && <Field label="Proveedor"><select value={ruleForm.supplier_id} onChange={e => onRuleChange({ supplier_id: e.target.value })}><option value="">Selecciona</option>{suppliers.map(s => <option key={s.id} value={s.id}>{s.name}</option>)}</select></Field>}{scope === 'GROUP' && <Field label="Grupo"><select value={ruleForm.commission_group_id} onChange={e => onRuleChange({ commission_group_id: e.target.value })}><option value="">Selecciona</option>{groups.map(g => <option key={g.id} value={g.id}>{g.name}</option>)}</select></Field>}{scope === 'PRODUCT' && <Field label="Producto"><input onChange={e => void onSearchProducts(e.target.value)} placeholder="Busca SKU o nombre" />{products.length > 0 && <select value={ruleForm.product_id} onChange={e => onRuleChange({ product_id: e.target.value })}><option value="">Selecciona</option>{products.map(p => <option key={p.id} value={p.id}>{p.sku} · {p.description}</option>)}</select>}</Field>}<Field label="Vendedor opcional"><select value={ruleForm.seller_profile_id} onChange={e => onRuleChange({ seller_profile_id: e.target.value })}><option value="">Todos</option>{sellers.filter(s => s.seller_profile_id).map(s => <option key={s.seller_profile_id} value={s.seller_profile_id!}>{s.seller_name}</option>)}</select></Field><Field label="Vigente desde"><input type="date" value={ruleForm.valid_from} onChange={e => onRuleChange({ valid_from: e.target.value })} /></Field><Field label="Prioridad"><input type="number" value={ruleForm.priority} onChange={e => onRuleChange({ priority: e.target.value })} /></Field><button disabled={busy} onClick={onSaveRule} className="btn-primary md:col-span-3"><Plus className="h-3.5 w-3.5" />Guardar regla</button></div><div className="overflow-auto rounded-xl border border-theme-border"><table className="min-w-[700px] w-full text-xs"><thead><tr><th>Ámbito</th><th>Tipo</th><th>%</th><th>Vigencia</th><th>Estado</th><th></th></tr></thead><tbody>{rules.map(rule => <tr key={rule.id}><td>{rule.rule_scope}</td><td>{rule.rule_type}</td><td>{rule.commission_percent}%</td><td>{rule.valid_from}</td><td>{rule.is_active ? 'Activa' : 'Inactiva'}</td><td>{rule.is_active && <button disabled={busy} onClick={() => onDeactivateRule(rule.id)} className="btn-secondary">Desactivar</button>}</td></tr>)}</tbody></table></div></div> }

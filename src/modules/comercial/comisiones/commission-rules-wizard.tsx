'use client'

import { useEffect, useState } from 'react'
import { Check, ChevronLeft, ChevronRight, LoaderCircle, Plus, Search, X } from 'lucide-react'
import { archiveCommissionRuleBatch, createGuidedCommissionRule, getCommissionRuleBatchDetail, restoreCommissionRuleBatch, searchCommissionProducts, searchCommissionSuppliers, setCommissionRuleBatchActive, type CommissionGroup, type CommissionRule, type CommissionRuleType, type CommissionSeller } from '@/app/actions/comercial/commissions'
import { cn, parsePercent, formatPercent } from '@/lib/utils'

type Supplier = { id: string; name: string; rut: string | null; type_label?: string }
type Product = { id: string; sku: string; description: string; supplier_name: string | null }
type TargetMode = 'GENERAL' | 'SUPPLIER_ALL_PRODUCTS' | 'SUPPLIER_SELECTED_PRODUCTS' | 'EXISTING_GROUP' | 'SELECTED_PRODUCTS'
const stepNames = ['Identificación', 'Alcance y selección', 'Vendedores', 'Comisión', 'Resumen']
const today = () => new Date().toISOString().slice(0, 10)

export function CommissionRulesWizard({ sellers, groups, rules, onSaved, onError }: { sellers: CommissionSeller[]; groups: CommissionGroup[]; rules: CommissionRule[]; onSaved: () => Promise<void>; onError: (message: string) => void }) {
  const [step, setStep] = useState(0)
  const [name, setName] = useState('')
  const [description, setDescription] = useState('')
  const [from, setFrom] = useState(today)
  const [to, setTo] = useState('')
  const [mode, setMode] = useState<TargetMode>('SUPPLIER_ALL_PRODUCTS')
  const [allSellers, setAllSellers] = useState(true)
  const [sellerIds, setSellerIds] = useState<string[]>([])
  const [suppliers, setSuppliers] = useState<Supplier[]>([])
  const [supplierResults, setSupplierResults] = useState<Supplier[]>([])
  const [supplierQuery, setSupplierQuery] = useState('')
  const [products, setProducts] = useState<Product[]>([])
  const [productResults, setProductResults] = useState<Product[]>([])
  const [productQuery, setProductQuery] = useState('')
  const [groupIds, setGroupIds] = useState<string[]>([])
  const [type, setType] = useState<CommissionRuleType>('FIXED_PERCENT')
  const [percent, setPercent] = useState('')
  const [minQuantity, setMinQuantity] = useState('')
  const [maxQuantity, setMaxQuantity] = useState('')
  const [minAmount, setMinAmount] = useState('')
  const [maxAmount, setMaxAmount] = useState('')
  const [loading, setLoading] = useState<'suppliers' | 'products' | 'save' | null>(null)
  const eligibleSellers = sellers.filter(seller => seller.is_commissionable && seller.profile_active === true && seller.seller_profile_id)
  const supplierMode = mode === 'SUPPLIER_ALL_PRODUCTS' || mode === 'SUPPLIER_SELECTED_PRODUCTS'

  const validate = () => {
    if (step === 0 && !name.trim()) { onError('Ingresa un nombre para la condición.'); return false }
    if (step === 0 && (!from || to && to < from)) { onError(to && to < from ? 'La fecha final no puede ser anterior a la fecha inicial.' : 'Indica la fecha de inicio.'); return false }
    if (step === 2 && !allSellers && !sellerIds.length) { onError('Selecciona al menos un vendedor comisionable.'); return false }
    if (step === 1 && supplierMode && !suppliers.length) { onError('Selecciona al menos un proveedor.'); return false }
    if (step === 1 && (mode === 'SUPPLIER_SELECTED_PRODUCTS' || mode === 'SELECTED_PRODUCTS') && !products.length) { onError('Selecciona al menos un producto.'); return false }
    if (step === 1 && mode === 'EXISTING_GROUP' && !groupIds.length) { onError('Selecciona al menos un grupo de productos.'); return false }
    if (step === 3 && !percent) { onError('Indica un porcentaje de comisión.'); return false }
    if (step === 3 && type === 'RANGE_BY_QUANTITY' && !(Number(minQuantity) > 0)) { onError('Debes indicar una cantidad mínima.'); return false }
    if (step === 3 && type === 'RANGE_BY_AMOUNT' && !(Number(minAmount) > 0)) { onError('Debes indicar un monto mínimo.'); return false }
    return true
  }
  const add = <T extends { id: string }>(item: T, update: React.Dispatch<React.SetStateAction<T[]>>) => update(current => current.some(value => value.id === item.id) ? current : [...current, item])
  const next = () => { if (validate()) setStep(current => Math.min(current + 1, 4)) }
  const cancel = () => { setStep(0); setName(''); setDescription(''); setFrom(today()); setTo(''); setMode('SUPPLIER_ALL_PRODUCTS'); setAllSellers(true); setSellerIds([]); setSuppliers([]); setProducts([]); setGroupIds([]); setPercent(''); setMinQuantity(''); setMaxQuantity(''); setMinAmount(''); setMaxAmount('') }
  const findSuppliers = async () => { setLoading('suppliers'); try { setSupplierResults(await searchCommissionSuppliers(supplierQuery)) } catch { onError('No se pudieron buscar proveedores reales.') } finally { setLoading(null) } }
  const findProducts = async () => { if (productQuery.trim().length < 2) { setProductResults([]); return }; setLoading('products'); try { setProductResults(await searchCommissionProducts(productQuery, mode === 'SUPPLIER_SELECTED_PRODUCTS' ? suppliers.map(supplier => supplier.id) : undefined)) } catch { onError('No se pudieron buscar productos.') } finally { setLoading(null) } }
  const save = async () => { if (!validate()) return; setLoading('save'); try { await createGuidedCommissionRule({ ruleName: name, description, effectiveFrom: from, effectiveTo: to || undefined, appliesToAllSellers: allSellers, sellerProfileIds: sellerIds, targetMode: mode, supplierIds: suppliers.map(supplier => supplier.id), groupIds, productIds: products.map(product => product.id), commissionType: type, minQuantity: minQuantity ? Number(minQuantity) : null, maxQuantity: maxQuantity ? Number(maxQuantity) : null, minAmount: minAmount ? Number(minAmount) : null, maxAmount: maxAmount ? Number(maxAmount) : null, commissionPercent: parsePercent(percent) }); await onSaved(); cancel() } catch (error) { onError(error instanceof Error ? error.message : 'No se pudo guardar la condición.') } finally { setLoading(null) } }

  let content: React.ReactNode
  if (step === 0) content = <><Title value="1. Identificación y vigencia" help="Usa un nombre claro, por ejemplo: Promoción ANASAC agosto o Fiestas Patrias." /><div className="mt-4 grid gap-3 md:grid-cols-2 xl:grid-cols-4"><Field label="Nombre de regla o campaña"><input value={name} onChange={event => setName(event.target.value)} /></Field><Field label="Descripción opcional"><input value={description} onChange={event => setDescription(event.target.value)} /></Field><Field label="Vigente desde"><input type="date" value={from} onChange={event => setFrom(event.target.value)} /></Field><Field label="Vigente hasta opcional"><input type="date" min={from} value={to} onChange={event => setTo(event.target.value)} /></Field></div></>
  else if (step === 1) content = <ScopeAndSelection mode={mode} setMode={setMode} groups={groups} groupIds={groupIds} setGroupIds={setGroupIds} suppliers={suppliers} supplierResults={supplierResults} supplierQuery={supplierQuery} setSupplierQuery={setSupplierQuery} findSuppliers={findSuppliers} supplierLoading={loading === 'suppliers'} addSupplier={supplier => add(supplier, setSuppliers)} removeSupplier={id => setSuppliers(current => current.filter(item => item.id !== id))} products={products} productResults={productResults} productQuery={productQuery} setProductQuery={setProductQuery} findProducts={findProducts} productLoading={loading === 'products'} addProduct={product => add(product, setProducts)} removeProduct={id => setProducts(current => current.filter(item => item.id !== id))} />
  else if (step === 2) content = <><Title value="3. Vendedores" help="Define si la condición se aplica a todos los vendedores comisionables o solo a algunos." /><div className="mt-4 flex flex-wrap gap-3"><Mode selected={allSellers} onClick={() => setAllSellers(true)} title="Todos los vendedores comisionables" text="La condición se evaluará para todos." /><Mode selected={!allSellers} onClick={() => setAllSellers(false)} title="Seleccionar vendedores" text="Restringe la condición a vendedores específicos." /></div>{!allSellers && <div className="mt-4 grid gap-2 md:grid-cols-2">{eligibleSellers.map(seller => <label key={seller.seller_profile_id} className="flex items-center gap-2 rounded-lg border border-theme-border p-3 text-sm"><input type="checkbox" checked={sellerIds.includes(seller.seller_profile_id!)} onChange={() => setSellerIds(current => current.includes(seller.seller_profile_id!) ? current.filter(id => id !== seller.seller_profile_id) : [...current, seller.seller_profile_id!])} />{seller.seller_name}</label>)}</div>}</>
  else if (step === 3) content = <Commission type={type} setType={setType} percent={percent} setPercent={setPercent} minQuantity={minQuantity} setMinQuantity={setMinQuantity} maxQuantity={maxQuantity} setMaxQuantity={setMaxQuantity} minAmount={minAmount} setMinAmount={setMinAmount} maxAmount={maxAmount} setMaxAmount={setMaxAmount} />
  else content = <Summary name={name} mode={mode} supplierCount={suppliers.length} productCount={products.length} groupCount={groupIds.length} allSellers={allSellers} sellerCount={sellerIds.length} type={type} percent={percent} from={from} to={to} minQuantity={minQuantity} minAmount={minAmount} />

  return <section className="w-full space-y-4"><header><h3 className="text-base font-semibold">Nueva condición de comisión</h3><p className="mt-1 text-sm text-theme-text-muted">Define cuándo una venta debe pagar una comisión distinta a la comisión general.</p></header><div className="sim-card"><ol className="flex overflow-x-auto rounded-lg p-1">{stepNames.map((label, index) => <li key={label} className={cn('min-w-28 flex-1 px-2 py-1 text-center text-xs font-medium', index === step ? 'rounded bg-theme-accent-muted text-theme-text' : index < step ? 'text-theme-text' : 'text-theme-text-muted')}>{index + 1} {label}</li>)}</ol></div><section className="sim-card p-4">{content}</section><div className="flex justify-between gap-3"><button type="button" onClick={cancel} className="btn-secondary"><X className="h-3.5 w-3.5" />Cancelar</button><div className="flex gap-2">{step > 0 && <button type="button" onClick={() => setStep(current => current - 1)} className="btn-secondary"><ChevronLeft className="h-3.5 w-3.5" />Atrás</button>}{step < 4 ? <button type="button" onClick={next} className="btn-primary">Siguiente<ChevronRight className="h-3.5 w-3.5" /></button> : <button type="button" onClick={() => void save()} disabled={loading === 'save'} className="btn-primary">{loading === 'save' ? <><LoaderCircle className="h-3.5 w-3.5 animate-spin" />Guardando...</> : <><Check className="h-3.5 w-3.5" />Guardar condición</>}</button>}</div></div><ExistingRules rules={rules} onSaved={onSaved} onError={onError} /></section>
}

function ScopeAndSelection(props: Parameters<typeof Selection>[0]) { const supplierMode = props.mode === 'SUPPLIER_ALL_PRODUCTS' || props.mode === 'SUPPLIER_SELECTED_PRODUCTS'; return <><Title value="2. Alcance y selección" help="Elige qué quieres comisionar y configura la selección inmediatamente." /><div className="mt-4 grid gap-3 md:grid-cols-2 xl:grid-cols-4"><Mode selected={props.mode === 'GENERAL'} onClick={() => props.setMode('GENERAL')} title="General" text="Aplica si no existe una condición más específica." /><Mode selected={supplierMode} onClick={() => props.setMode('SUPPLIER_ALL_PRODUCTS')} title="Proveedor" text="Uno o más proveedores, todos sus productos o algunos." /><Mode selected={props.mode === 'EXISTING_GROUP'} onClick={() => props.setMode('EXISTING_GROUP')} title="Grupo de productos" text="Usa un grupo creado previamente." /><Mode selected={props.mode === 'SELECTED_PRODUCTS'} onClick={() => props.setMode('SELECTED_PRODUCTS')} title="Productos específicos" text="Elige productos individuales por SKU." /></div><div className="mt-5 border-t border-theme-border pt-5"><Selection {...props} /></div></> }
function Selection(props: { mode: TargetMode; setMode: (mode: TargetMode) => void; groups: CommissionGroup[]; groupIds: string[]; setGroupIds: (ids: string[]) => void; suppliers: Supplier[]; supplierResults: Supplier[]; supplierQuery: string; setSupplierQuery: (query: string) => void; findSuppliers: () => Promise<void>; supplierLoading: boolean; addSupplier: (supplier: Supplier) => void; removeSupplier: (id: string) => void; products: Product[]; productResults: Product[]; productQuery: string; setProductQuery: (query: string) => void; findProducts: () => Promise<void>; productLoading: boolean; addProduct: (product: Product) => void; removeProduct: (id: string) => void }) { const supplierMode = props.mode === 'SUPPLIER_ALL_PRODUCTS' || props.mode === 'SUPPLIER_SELECTED_PRODUCTS'; if (props.mode === 'GENERAL') return <><Title value="Selección" help="Esta condición aplicará de forma general, salvo que exista una regla más específica." /></>; if (props.mode === 'EXISTING_GROUP') return <><Title value="Selección de grupos" help="Elige los grupos que usará la condición." />{props.groups.length ? <div className="mt-4 grid gap-2 md:grid-cols-2">{props.groups.map(group => <label key={group.id} className="flex items-center gap-2 rounded-lg border border-theme-border p-3"><input type="checkbox" checked={props.groupIds.includes(group.id)} onChange={() => props.setGroupIds(props.groupIds.includes(group.id) ? props.groupIds.filter(id => id !== group.id) : [...props.groupIds, group.id])} /><span><b>{group.name}</b><small className="block text-theme-text-muted">{group.description || 'Sin descripción'}</small></span></label>)}</div> : <p className="mt-4 text-sm text-theme-text-muted">No hay grupos creados. Crea un grupo antes de usar esta opción.</p>}</>; return <><Title value={supplierMode ? 'Selección de proveedor' : 'Selección de productos'} help={supplierMode ? 'Busca y selecciona proveedores reales de PetGroup.' : 'Busca productos por SKU o descripción.'} />{supplierMode && <><SearchBox value={props.supplierQuery} setValue={props.setSupplierQuery} search={props.findSuppliers} loading={props.supplierLoading} placeholder="Buscar proveedor real" /><SupplierResults items={props.supplierResults} selected={new Set(props.suppliers.map(item => item.id))} add={props.addSupplier} /><Chips items={props.suppliers} label={item => item.name} remove={props.removeSupplier} empty="Selecciona uno o más proveedores reales." />{props.suppliers.length > 0 && <div className="mt-3 rounded-lg border border-theme-border p-3"><p className="text-sm font-medium">¿La comisión aplica a todos los productos de estos proveedores?</p><div className="mt-3 flex gap-2"><button type="button" onClick={() => props.setMode('SUPPLIER_ALL_PRODUCTS')} className={props.mode === 'SUPPLIER_ALL_PRODUCTS' ? 'btn-primary' : 'btn-secondary'}>Sí, todos los productos</button><button type="button" onClick={() => props.setMode('SUPPLIER_SELECTED_PRODUCTS')} className={props.mode === 'SUPPLIER_SELECTED_PRODUCTS' ? 'btn-primary' : 'btn-secondary'}>No, solo algunos productos</button></div></div>}</>}{(!supplierMode || props.mode === 'SUPPLIER_SELECTED_PRODUCTS') && <div className="mt-4"><SearchBox value={props.productQuery} setValue={props.setProductQuery} search={props.findProducts} loading={props.productLoading} placeholder="Buscar por SKU o descripción" /><ProductResults items={props.productResults} selected={new Set(props.products.map(item => item.id))} add={props.addProduct} /><Chips items={props.products} label={item => `${item.sku} · ${item.description}`} remove={props.removeProduct} empty="No hay productos seleccionados." /><p className="mt-3 text-sm text-theme-text-muted">Cada SKU se evalúa individualmente. No se suman todos los productos del proveedor para cumplir el mínimo.</p></div>}</> }
function Commission(props: { type: CommissionRuleType; setType: (type: CommissionRuleType) => void; percent: string; setPercent: (value: string) => void; minQuantity: string; setMinQuantity: (value: string) => void; maxQuantity: string; setMaxQuantity: (value: string) => void; minAmount: string; setMinAmount: (value: string) => void; maxAmount: string; setMaxAmount: (value: string) => void }) { return <><Title value="5. Tipo de comisión" help="Las condiciones variables se evalúan por SKU individual." /><div className="mt-4 grid gap-3 md:grid-cols-3"><Mode selected={props.type === 'FIXED_PERCENT'} onClick={() => props.setType('FIXED_PERCENT')} title="Comisión fija" text="Un porcentaje fijo por venta." /><Mode selected={props.type === 'RANGE_BY_QUANTITY'} onClick={() => props.setType('RANGE_BY_QUANTITY')} title="Variable por cantidad" text="Exige una cantidad mínima por SKU." /><Mode selected={props.type === 'RANGE_BY_AMOUNT'} onClick={() => props.setType('RANGE_BY_AMOUNT')} title="Variable por monto" text="Exige un neto mínimo por SKU." /></div><div className="mt-4 grid gap-3 md:grid-cols-3">{props.type === 'RANGE_BY_QUANTITY' && <><Field label="Cantidad mínima"><input type="number" min="1" value={props.minQuantity} onChange={event => props.setMinQuantity(event.target.value)} /></Field><Field label="Cantidad máxima opcional"><input type="number" min="1" value={props.maxQuantity} onChange={event => props.setMaxQuantity(event.target.value)} /></Field></>}{props.type === 'RANGE_BY_AMOUNT' && <><Field label="Monto mínimo"><input type="number" min="1" value={props.minAmount} onChange={event => props.setMinAmount(event.target.value)} /></Field><Field label="Monto máximo opcional"><input type="number" min="1" value={props.maxAmount} onChange={event => props.setMaxAmount(event.target.value)} /></Field></>}<Field label="Porcentaje de comisión"><input type="text" inputMode="decimal" value={props.percent} onChange={event => props.setPercent(event.target.value)} placeholder="Ej: 2,25" /></Field></div>{props.type !== 'FIXED_PERCENT' && <p className="mt-3 text-sm text-theme-text-muted">Cada producto incluido debe alcanzar esta condición dentro del período para comisionar. No se suman los productos del proveedor.</p>}</> }
function Summary({ name, mode, supplierCount, productCount, groupCount, allSellers, sellerCount, type, percent, from, to, minQuantity, minAmount }: { name: string; mode: TargetMode; supplierCount: number; productCount: number; groupCount: number; allSellers: boolean; sellerCount: number; type: CommissionRuleType; percent: string; from: string; to: string; minQuantity: string; minAmount: string }) { const target = mode === 'GENERAL' ? 'General' : mode === 'SUPPLIER_ALL_PRODUCTS' ? `Todos los productos de ${supplierCount} proveedor(es)` : mode === 'SUPPLIER_SELECTED_PRODUCTS' ? `${productCount} productos seleccionados` : mode === 'EXISTING_GROUP' ? `${groupCount} grupo(s)` : `${productCount} productos específicos`; const condition = type === 'FIXED_PERCENT' ? 'Sin condición adicional' : type === 'RANGE_BY_QUANTITY' ? `Cada SKU debe alcanzar ${minQuantity} unidades` : `Cada SKU debe alcanzar $${minAmount} netos`;   const percentVal = parsePercent(percent)
  return <><Title value="6. Resumen antes de guardar" help="Revisa la condición antes de crearla." /><dl className="mt-4 grid gap-4 md:grid-cols-2 xl:grid-cols-3"><Item label="Nombre" value={name} /><Item label="Aplica sobre" value={target} /><Item label="Vendedores" value={allSellers ? 'Todos los vendedores comisionables' : `${sellerCount} vendedor(es)`} /><Item label="Tipo" value={type === 'FIXED_PERCENT' ? 'Comisión fija' : type === 'RANGE_BY_QUANTITY' ? 'Variable por cantidad' : 'Variable por monto'} /><Item label="Condición" value={condition} /><Item label="Comisión" value={formatPercent(percentVal)} /><Item label="Vigencia" value={`${from}${to ? ` a ${to}` : ' en adelante'}`} /></dl></> }
function ExistingRules({ rules, onSaved, onError }: { rules: CommissionRule[]; onSaved: () => Promise<void>; onError: (message: string) => void }) {
  const [busy, setBusy] = useState<string | null>(null)
  const [detail, setDetail] = useState<Awaited<ReturnType<typeof getCommissionRuleBatchDetail>> | null>(null)
  const [status, setStatus] = useState<'ALL' | 'ACTIVE' | 'INACTIVE'>('ALL')
  const [showArchived, setShowArchived] = useState(false)
  const [actionMessage, setActionMessage] = useState('')
  useEffect(() => { if (!actionMessage) return; const timer = window.setTimeout(() => { setActionMessage(''); onError('') }, 2500); return () => window.clearTimeout(timer) }, [actionMessage, onError])
  const batches = Array.from(rules.reduce((map, rule) => { const id = rule.rule_batch_id || rule.id; map.set(id, [...(map.get(id) || []), rule]); return map }, new Map<string, CommissionRule[]>()).entries())
  const setActive = async (id: string, isActive: boolean) => { setActionMessage(''); setBusy(id); try { await setCommissionRuleBatchActive(id, isActive); await onSaved(); const message = isActive ? 'Condición activada correctamente.' : 'Condición desactivada correctamente.'; setActionMessage(message); onError(message) } catch { onError(isActive ? 'No se pudo activar la condición.' : 'No se pudo desactivar la condición.') } finally { setBusy(null) } }
  const view = async (id: string, rule: CommissionRule) => { if (!rule.rule_batch_id) return onError('Esta condición antigua no tiene un lote agrupado para mostrar detalle.'); setBusy(id); try { setDetail(await getCommissionRuleBatchDetail(rule.rule_batch_id)) } catch (error) { onError(error instanceof Error ? error.message : 'No se pudo cargar el detalle.') } finally { setBusy(null) } }
  const archive = async (id: string) => { if (!window.confirm('Esta condición se ocultará de la lista principal y dejará de aplicar. No será eliminada.')) return; setActionMessage(''); setBusy(id); try { await archiveCommissionRuleBatch({ ruleBatchId: id }); await onSaved(); const message = 'Condición archivada correctamente.'; setActionMessage(message); onError(message) } catch { onError('No se pudo archivar la condición.') } finally { setBusy(null) } }
  const restore = async (id: string) => { if (!window.confirm('La condición volverá a la lista como inactiva. Luego podrás activarla manualmente.')) return; setActionMessage(''); setBusy(id); try { await restoreCommissionRuleBatch(id); await onSaved(); const message = 'Condición restaurada correctamente.'; setActionMessage(message); onError(message) } catch { onError('No se pudo restaurar la condición.') } finally { setBusy(null) } }
  const visible = batches.filter(([, batch]) => { const rule = batch[0]; if (rule.is_archived && !showArchived) return false; if (status === 'ACTIVE') return !rule.is_archived && rule.is_active; if (status === 'INACTIVE') return !rule.is_archived && !rule.is_active; return true })
  return <section className="sim-card p-4"><div className="flex flex-wrap items-center justify-between gap-3"><h3 className="font-semibold">Condiciones existentes</h3><div className="flex items-center gap-3"><select className="h-7 text-[11px]" value={status} onChange={event => setStatus(event.target.value as typeof status)}><option value="ALL">Todas</option><option value="ACTIVE">Activas</option><option value="INACTIVE">Inactivas</option></select><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={showArchived} onChange={event => setShowArchived(event.target.checked)} />Mostrar archivadas</label></div></div>{visible.length ? <div className="mt-3 overflow-x-auto"><table className="w-full min-w-[820px] text-sm"><thead><tr><th>Nombre</th><th>Aplica sobre</th><th>Tipo</th><th>Comisión</th><th>Vigencia</th><th>Estado</th><th /></tr></thead><tbody>{visible.map(([id, batch]) => { const rule = batch[0]; const archived = Boolean(rule.is_archived); return <tr key={id}><td><b>{rule.rule_name || 'Condición de comisión'}</b></td><td>{rule.rule_scope === 'GENERAL' ? 'General' : rule.rule_scope === 'SUPPLIER' ? 'Proveedor' : rule.rule_scope === 'GROUP' ? 'Grupo' : `${batch.length} producto(s)`}</td><td>{rule.rule_type === 'FIXED_PERCENT' ? 'Fija' : rule.rule_type === 'RANGE_BY_QUANTITY' ? 'Por cantidad' : 'Por monto'}</td><td>{rule.commission_percent != null ? formatPercent(Number(rule.commission_percent)) : '-'}</td><td>{rule.valid_from}{rule.valid_to ? ` a ${rule.valid_to}` : ' en adelante'}</td><td><span className="rounded-full border border-theme-border px-2 py-1 text-xs">{archived ? 'Archivada' : rule.is_active ? 'Activa' : 'Inactiva'}</span></td><td className="whitespace-nowrap"><button type="button" disabled={busy === id} onClick={() => void view(id, rule)} className="btn-secondary">Ver detalle</button>{rule.rule_batch_id && (archived ? <button type="button" disabled={busy === id} onClick={() => void restore(id)} className="btn-secondary ml-2">Restaurar</button> : <><button type="button" disabled={busy === id} onClick={() => void setActive(id, !rule.is_active)} className="btn-secondary ml-2">{busy === id ? 'Guardando...' : rule.is_active ? 'Desactivar' : 'Activar'}</button><button type="button" disabled={busy === id} onClick={() => void archive(id)} className="btn-secondary ml-2">Archivar</button></>)}</td></tr> })}</tbody></table></div> : <p className="mt-3 text-sm text-theme-text-muted">No hay condiciones que coincidan con los filtros.</p>}{detail && <RuleDetail detail={detail} onClose={() => setDetail(null)} />}</section>
}
function RuleDetail({ detail, onClose }: { detail: Awaited<ReturnType<typeof getCommissionRuleBatchDetail>>; onClose: () => void }) { const type = detail.type === 'FIXED_PERCENT' ? 'Comisión fija' : detail.type === 'RANGE_BY_QUANTITY' ? 'Variable por cantidad' : 'Variable por monto'; return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4"><section className="max-h-[90vh] w-full max-w-3xl overflow-auto rounded-xl border border-theme-border bg-theme-surface p-5 text-theme-text"><div className="flex items-start justify-between gap-4"><div><h3 className="text-lg font-semibold">{detail.name}</h3><p className="mt-1 text-sm text-theme-text-muted">{detail.description || 'Sin descripción'}</p></div><button type="button" onClick={onClose} className="btn-secondary">Cerrar</button></div><dl className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-3"><Detail label="Estado" value={detail.isActive ? 'Activa' : 'Inactiva'} /><Detail label="Vigencia" value={`${detail.validFrom}${detail.validTo ? ` a ${detail.validTo}` : ' en adelante'}`} /><Detail label="Comisión" value={formatPercent(detail.commissionPercent)} /><Detail label="Tipo" value={type} /><Detail label="Alcance" value={detail.scope === 'GENERAL' ? 'General' : detail.scope === 'SUPPLIER' ? 'Proveedor' : detail.scope === 'GROUP' ? 'Grupo de productos' : 'Productos seleccionados'} />{detail.type === 'RANGE_BY_QUANTITY' && <Detail label="Cantidad" value={`${detail.minQuantity ?? 0}${detail.maxQuantity ? ` a ${detail.maxQuantity}` : '+'}`} />}{detail.type === 'RANGE_BY_AMOUNT' && <Detail label="Monto" value={`${detail.minAmount ?? 0}${detail.maxAmount ? ` a ${detail.maxAmount}` : '+'}`} />}</dl><div className="mt-5 grid gap-5 lg:grid-cols-2"><DetailList title="Vendedores" items={detail.sellers.map(seller => `${seller.name} (${seller.bsaleId})`)} empty="Todos los vendedores comisionables" /><DetailList title="Proveedores" items={detail.suppliers.map(supplier => supplier.name)} empty="No aplica" /><DetailList title="Grupos" items={detail.groups.map(group => group.name)} empty="No aplica" /><div><h4 className="font-semibold">Productos incluidos</h4>{detail.products.length ? <div className="mt-2 overflow-x-auto rounded-lg border border-theme-border"><table className="w-full text-sm"><thead><tr><th>SKU</th><th>Producto</th><th>Proveedor</th></tr></thead><tbody>{detail.products.map(product => <tr key={product.sku}><td className="font-mono text-xs">{product.sku}</td><td>{product.name}</td><td>{product.supplierName}</td></tr>)}</tbody></table></div> : <p className="mt-2 text-sm text-theme-text-muted">No aplica</p>}</div></div><p className="mt-5 text-xs text-theme-text-muted">Para cambiar productos, proveedor, vendedor, alcance o tipo de comisión, desactiva esta condición y crea una nueva.</p></section></div> }
function Detail({ label, value }: { label: string; value: string }) { return <div><dt className="text-xs font-semibold uppercase text-theme-text-muted">{label}</dt><dd className="mt-1 text-sm">{value}</dd></div> }
function DetailList({ title, items, empty }: { title: string; items: string[]; empty: string }) { return <div><h4 className="font-semibold">{title}</h4>{items.length ? <ul className="mt-2 space-y-1 text-sm">{items.map(item => <li key={item}>{item}</li>)}</ul> : <p className="mt-2 text-sm text-theme-text-muted">{empty}</p>}</div> }
function SearchBox({ value, setValue, search, loading, placeholder }: { value: string; setValue: (value: string) => void; search: () => Promise<void>; loading: boolean; placeholder: string }) { return <div className="mt-4 flex gap-2"><input value={value} onChange={event => setValue(event.target.value)} onKeyDown={event => { if (event.key === 'Enter') { event.preventDefault(); void search() } }} placeholder={placeholder} /><button type="button" onClick={() => void search()} className="btn-secondary">{loading ? <LoaderCircle className="h-3.5 w-3.5 animate-spin" /> : <Search className="h-3.5 w-3.5" />}Buscar</button></div> }
function SupplierResults({ items, selected, add }: { items: Supplier[]; selected: Set<string>; add: (item: Supplier) => void }) { return <div className="mt-3 overflow-x-auto rounded-lg border border-theme-border"><table className="w-full min-w-[560px] text-sm"><thead><tr><th>Proveedor</th><th>RUT</th><th>Tipo</th><th>Acción</th></tr></thead><tbody>{items.map(item => <tr key={item.id}><td>{item.name}</td><td>{item.rut || 'Sin RUT'}</td><td>{item.type_label || 'Proveedor real'}</td><td>{selected.has(item.id) ? 'Agregado' : <button type="button" onClick={() => add(item)} className="btn-secondary"><Plus className="h-3.5 w-3.5" />Agregar</button>}</td></tr>)}</tbody></table>{!items.length && <p className="p-4 text-center text-sm text-theme-text-muted">Sin proveedores reales para mostrar.</p>}</div> }
function ProductResults({ items, selected, add }: { items: Product[]; selected: Set<string>; add: (item: Product) => void }) { return <div className="mt-3 overflow-x-auto rounded-lg border border-theme-border"><table className="w-full min-w-[600px] text-sm"><thead><tr><th>SKU</th><th>Producto</th><th>Proveedor</th><th>Acción</th></tr></thead><tbody>{items.map(item => <tr key={item.id}><td className="font-mono text-xs">{item.sku}</td><td>{item.description}</td><td>{item.supplier_name || 'Sin proveedor asociado'}</td><td>{selected.has(item.id) ? 'Agregado' : <button type="button" onClick={() => add(item)} className="btn-secondary"><Plus className="h-3.5 w-3.5" />Agregar</button>}</td></tr>)}</tbody></table>{!items.length && <p className="p-4 text-center text-sm text-theme-text-muted">Sin productos para mostrar.</p>}</div> }
function Chips<T extends { id: string }>({ items, label, remove, empty }: { items: T[]; label: (item: T) => string; remove: (id: string) => void; empty: string }) { return <div className="mt-3 rounded-lg border border-theme-border p-3">{items.length ? <div className="flex flex-wrap gap-2">{items.map(item => <span key={item.id} className="inline-flex items-center gap-2 rounded-full bg-theme-accent-muted px-3 py-1 text-xs">{label(item)}<button type="button" onClick={() => remove(item.id)}>Quitar</button></span>)}</div> : <p className="text-sm text-theme-text-muted">{empty}</p>}</div> }
function Title({ value, help }: { value: string; help: string }) { const title = value === '5. Tipo de comisión' ? '4. Tipo de comisión' : value === '6. Resumen antes de guardar' ? '5. Resumen antes de guardar' : value; return <><h4 className="font-semibold">{title}</h4><p className="mt-1 text-sm text-theme-text-muted">{help}</p></> }
function Mode({ selected, onClick, title, text }: { selected: boolean; onClick: () => void; title: string; text: string }) { return <button type="button" onClick={onClick} className={cn('rounded-lg border p-3 text-left', selected ? 'border-theme-accent bg-theme-accent-muted' : 'border-theme-border bg-theme-surface')}><b className="block text-sm">{title}</b><span className="mt-1 block text-xs text-theme-text-muted">{text}</span></button> }
function Field({ label, children }: { label: string; children: React.ReactNode }) { return <label className="text-sm font-medium">{label}<span className="mt-1 block">{children}</span></label> }
function Item({ label, value }: { label: string; value: string }) { return <div><dt className="text-xs font-semibold uppercase text-theme-text-muted">{label}</dt><dd className="mt-1">{value}</dd></div> }
